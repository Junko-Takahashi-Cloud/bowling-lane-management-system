# 第三弾:店舗・スタッフ管理システム 設計仕様書

## 全体方針

- 施設規模:4レーン(アメリカン方式、1-2番・3-4番のペア構成)
- スタッフ:少人数運営(店長+スタッフ数名)、階層管理なしの簡易PIN認証
- 料金体系:時間単価制(1ゲーム単位ではなく、レーン予約時間×時間料金)
- 教室(全5回)が構想の中核。一般利用は教室修了者限定
- 大会・トーナメントは開催しない

---

## 第二弾との接続ルール(確定・最重要)

第三弾は第二弾の既存モデル(`User`, `LaneSet`, `Reservation`, `ClassCourse`, `ClassSession`, `ClassEnrollment`, `GroupReservationDetail`, `ClassGroupEnrollment`)を土台に**拡張**する。作り直しはしない。

第二弾コードの実物確認(models.py)を踏まえ、Claudeが以下3点を判定し確定した。

1. **User と staff は別テーブル**。Userはお客様認証、staffはスタッフ認証で用途が異なるため。
2. **ClassSession は置き換えず拡張する**。既存の`class_sessions`(`session_number`, `date`, `start_time`, `end_time`, `status`)は第三弾が必要としていた回次情報とほぼ一致しているため、新規テーブルは作らず、レーンペア情報を追加する形で対応する。
3. **class_group は新設しない**。当初はPhase3-1で新しいテーブルとして`class_group`(教室グループ・募集枠)を設計する案だったが、既存の`ClassCourse`(募集枠本体:first_date, capacity, instructor_name, status)・`ClassEnrollment`(個人申込)・`ClassGroupEnrollment`(職場グループ申込)の3テーブルで同等以上の責務を表現できることが判明したため、新設しない。既存コードの方が個人申込と職場グループ申込を最初から分離しており、第三弾案より精緻だったため。

また、`LaneSet`(第二弾・セット単位管理)はそのまま維持し、個別レーンの状態管理(空き/故障中/メンテ中)が必要な第三弾の要件は、新規`lanes`テーブルを追加して`lane_set_id`で`LaneSet`と紐付ける形で対応する。`LaneSet`自体への変更は行わない。

---

## Phase 3-1(MVP・最優先)

### 目的
現場のオペレーションを止めないための最小構成。予約→受付→レンタル→利用→会計→退店の一連の流れを実現する。

### テーブル定義

**staff(スタッフ・新規)**
| カラム | 型 | 説明 |
|---|---|---|
| id | PK | |
| name | 文字列 | スタッフ氏名 |
| pin_code | 文字列(4桁) | ログイン用PIN |
| is_active | 真偽値 | 在籍中かどうか |

**lanes(個別レーン・新規、既存LaneSetと共存)**
| カラム | 型 | 説明 |
|---|---|---|
| id | PK | |
| lane_set_id | FK | 既存`lane_sets`テーブルへの紐付け |
| lane_number | 整数 | 実際のレーン番号 |
| pair_number | 整数 | アメリカン方式のペア番号(1-2番→1、3-4番→2) |
| status | enum | 空き/使用中/故障中/メンテ中(ペア内でも独立管理、自動連動なし) |

※既存`lane_sets`(A/Bセット単位管理)はそのまま維持。`lanes`は個別レーンの運用状態を管理する新規テーブルとして追加する。

**教室関連(新設なし・既存モデルを使用)**

「class_group」は新設しない。以下の既存テーブルで代替する:
- `class_courses`(募集枠本体。first_date, capacity, instructor_name, status)
- `class_enrollments`(個人申込。最大10名の定員チェック対象)
- `class_group_enrollments`(職場グループ申込。人数上限なし、定員チェック対象外)

カリキュラム:1週目=基本動作、2週目=ストライク、3週目=スペア、4週目=復習、5週目=卒業記録会

**class_sessions(教室の回次・既存を拡張)**
| カラム | 型 | 説明 |
|---|---|---|
| class_session_id | PK(既存) | |
| course_id | FK(既存) | `class_courses`への紐付け |
| session_number | 整数(既存) | 第1〜5回 |
| date | 日付(既存) | 開催日 |
| start_time / end_time | 時刻(既存) | |
| status | enum(既存) | scheduled/cancelled |
| lane_pair | 整数(追加) | 使用するレーンペアの**先頭レーン番号**を保持(例:1→1・2番レーン、3→3・4番レーン)。DB値だけで使用レーンを判別できる方式として確定 |

**class_attendance(出欠・新規)**
| カラム | 型 | 説明 |
|---|---|---|
| id | PK | |
| class_session_id | FK | 既存`class_sessions.class_session_id`への紐付け |
| attendance_status | enum | 全員出席/一部欠席・継続開催/一部欠席・全員次週へスライド(スライドは職場グループのみ選択可) |
| absent_note | 文字列(null可) | 欠席者メモ |

※出欠は個人単位ではなく**グループ単位**で記録。振替(スライド)は職場グループのみ可、個人予約は不可(休んだ回は欠席のまま次回へ)。

**checkin(来店受付・利用)**
| カラム | 型 | 説明 |
|---|---|---|
| id | PK | |
| reservation_id | FK(null可) | 予約への紐付け(体験利用はその場で作成) |
| lane_id | FK | 使用レーン(1予約につきレーン数分の行) |
| status | enum | 来店/利用中/退店 |
| checkin_time | 日時 | |
| checkout_time | 日時(null可) | |
| is_trial | 真偽値 | 体験利用フラグ |
| shoes_rental | 真偽値 | シューズレンタル(課金対象) |
| ball_rental | 真偽値 | ボールレンタル(課金対象。差別化戦略としてあえて課金) |

※飛び込み客対策として「体験利用」枠を新設(簡易オリエンテーション後、その場で予約レコード作成)。

**payment(決済)**
| カラム | 型 | 説明 |
|---|---|---|
| id | PK | |
| payer_type | enum | class(教室)/checkin(時間貸し) |
| course_id | FK(null可) | 既存`class_courses`への紐付け(教室支払いの場合) |
| checkin_id | FK(null可) | |
| payment_timing | enum | 先払い(class)/後払い(checkin) |
| amount | 数値 | 時間単価×利用時間で算出(サークル・グループは人数で割らず「1人1時間単価固定」) |
| payment_method | enum | 現金/カード/電子決済/未収 |
| is_paid | 真偽値 | |
| paid_at | 日時(null可) | |

※教室利用時、レンタル代は教室費に込みで個別課金しない。

**pricing_rule(料金マスタ)**
| カラム | 型 | 説明 |
|---|---|---|
| id | PK | |
| item_type | enum | lane_time/shoes_rental/ball_rental/class |
| price | 数値 | 単価 |

### 画面(Phase3-1)
- スタッフPINログイン画面
- 当日ダッシュボード(4レーンの状態+当日予約一覧、レーンはペア単位で表示)
- 受付(チェックイン)画面(個人・教室グループ・サークルグループ対応、レンタル選択)
- 会計(チェックアウト)画面(グループは1人ずつ個別精算)
- レーン状態変更画面(空き/故障中/メンテ中の切り替え、状況メモ付き)
- 教室出欠登録画面(3択:全員出席/一部欠席継続/一部欠席スライド)
- 教室募集枠作成画面(スタッフが第1回日時・レーンペア・担当コーチを指定→第2〜5回自動設定)

---

## Phase 3-2

### 1. コーチのシフト管理

**coach_shift(定期勤務パターン)**
| カラム | 型 | 説明 |
|---|---|---|
| id | PK | |
| staff_id | FK | |
| day_of_week | enum | 月〜日 |
| start_time | 時刻 | |
| end_time | 時刻 | |

**coach_shift_exception(日付単位の例外)**
| カラム | 型 | 説明 |
|---|---|---|
| id | PK | |
| staff_id | FK | |
| exception_date | 日付 | |
| type | enum | 休み/臨時出勤 |
| start_time | 時刻(null可) | 臨時出勤時のみ |
| end_time | 時刻(null可) | 臨時出勤時のみ |

※複数コーチ体制を想定(プロボウラーの臨時参加、従業員のコーチ昇格などを見込む)。休みは不定期のため例外テーブルで柔軟に対応。

**staff_attendance(タイムカード・実績記録)**
| カラム | 型 | 説明 |
|---|---|---|
| id | PK | |
| staff_id | FK | |
| clock_in | 日時 | |
| clock_out | 日時(null可) | 早退時はここに記録 |
| note | 文字列(null可) | 早退理由等 |

### 2. 予約時間帯の自動チェック機能
グループ予約の申込時、および個人教室募集枠の作成時の両方に適用。
判定ロジック:
1. `coach_shift_exception`で休みでないか確認
2. `coach_shift`でその曜日・時間が勤務時間内か確認
3. `checkin`/`class_session`でレーンペアが空いているか確認
→ 全て満たせば予約可能

### 3. レーンアサイン変更
2つの粒度を併用する。
- **当日開放(lane_pair_assignment)**:教室が入っていない時間帯だけ一時的に競技者用へ開放
  | カラム | 型 | 説明 |
  |---|---|---|
  | id | PK | |
  | lane_pair | 整数 | 使用するレーンペアの先頭レーン番号(1→1・2番、3→3・4番) |
  | date | 日付 | |
  | start_time | 時刻 | |
  | end_time | 時刻 | |
  | purpose | enum | 教室用/競技者開放 |
- **締切ベースの自動開放**:既存`class_courses`に`application_deadline`(申込締切日)カラムを追加し、締切までに人数が集まらなければ自動的にキャンセルし、競技者用の通常予約(4レーンフル開放)として開放

### 4. 修了生フラグ判定
グループ単位で判定(個人追跡はしない)。全5回を修了したグループを「修了生」として扱い、一般利用枠(教室未受講者は利用不可)の判定に使う。

### 5. キャンセル・変更対応
- キャンセル料は徴収しない(後払い・現地払いのため請求手段がない)
- 教室の先払いは1回目来店時に現地で受領。途中キャンセルの返金なし
- オンライン事前決済(クレカ事前登録)は個人情報リスクを避けるため将来的にも導入しない
- 無断・直前キャンセルは「お金」ではなく「利用資格」で管理

**no_show_record**
| カラム | 型 | 説明 |
|---|---|---|
| id | PK | |
| email | 文字列 | メールアドレス単位で識別 |
| target_type | enum | checkin/class_course |
| target_id | FK | |
| reason | enum | 無断キャンセル/直前キャンセル/自己都合キャンセル |
| recorded_at | 日時 | |

累積回数が一定を超えたら次回以降の予約を制限する運用。

### 6. 売上・利用履歴管理
既存の`payment`テーブルを集計して表示。新規テーブル不要。
- デフォルトは日別表示、週・月単位への切り替えも可能
- 内訳(レーン利用/シューズレンタル/ボールレンタル/教室)、支払方法別集計
- CSVエクスポート機能

---

## Phase 3-3(後回し)
- メンテナンス管理(オイルパターン・消耗品等の記録。初期は紙運用で代替可)

---

## 補足:予約フォームの仕様
- Web予約フォームに電話番号を任意項目として追加(メールアドレスの電話口聞き取りリスク軽減のため)。ただし折り返し確認は基本行わない
