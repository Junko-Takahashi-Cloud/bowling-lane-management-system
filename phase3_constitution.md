# 第三弾 4AI共通実装ルール(憲法)

## 0. この文書の目的

第三弾「店舗・スタッフ・コーチ管理」の実装において、Claude・ChatGPT・Gemini・Copilotの4AIが同一の設計基準を共有し、既存の第一弾・第二弾を壊さずに第三弾を拡張するための共通ルールを定める。

この文書は、4AIが実装時に共通して参照する「憲法」とする。

---

## 1. 開発の基本原則

**原則1:既存システムを勝手に壊さない**
第一弾・第二弾ですでに実装済みのモデル・API・機能を尊重する。第三弾で必要な機能は、原則として既存構造を土台にして追加・拡張する。既存モデルを削除・置換する場合は、独断で実施しない。

**原則2:仕様書にない変更は勝手に確定しない**
仕様書・共通ルール・既存コードから判断できない事項が発生した場合、「変更提案」として提示する。AIが独断で仕様を追加・変更・削除してはいけない。

**原則3:AI間で判断が分かれた場合**
AIごとに異なる設計判断をしない。判断が必要な場合はChatGPT(司令塔)に共有し、必要に応じてClaudeの設計レビューを受ける。

**原則4:実装前に設計を確認する**
ER図・テーブル定義・既存モデル・API仕様・担当範囲を確認してから実装する。不明点を推測して実装しない。

---

## 2. 4AIの役割分担

### ChatGPT(チャッピ):司令塔
- 全体設計の把握、AI間の回答・提案の整理、設計上の矛盾の整理
- 変更提案の確認、実装順序の整理、各AIへの作業指示
- 実装結果の統合確認、「確定事項」と「変更提案」の区別
- 必要に応じてClaudeへ設計レビューを依頼
- 権限:4AI間の設計判断を整理する司令塔として機能する。ただし仕様書を勝手に変更するのではなく、変更が必要な場合は変更理由を明確にする

### Claude:設計基準+担当領域実装+レビュー
- ER図・テーブル定義の実装上の確認、SQLAlchemyモデル構成、Pydantic Schema構成
- API命名・レスポンス形式、ディレクトリ構成、命名規則、Enum・statusの値
- 各機能の責務分担、担当領域の実装、他AIの実装に対する設計レビュー
- **担当実装範囲:グループC「スタッフ管理」(staffテーブル)**。コーチのシフト管理はPhase3-2のためPhase3-1では対象外
- 禁止事項:仕様書にない変更を独断で実装・確定しない。必要な場合は「変更提案」として提示する

### staff実装の確定事項(グループC・実装レビュー済み)

Claudeが実装したstaffドメイン(モデル・スキーマ・認証・API)について、以下4点をチャッピとの確認を経て確定した。

1. **`pin_code`ではなく`pin_hash`で保存する**。仕様書当初案の平文`pin_code`カラムから変更。既存`User.password_hash`と同じ設計思想(bcryptハッシュ化)に統一する
2. **StaffのJWTトークンはclaimキーを`staff_id`とする**(既存Userトークンの`sub`とは別立て)。UserトークンとStaffトークンの取り違えを構造的に防ぐため
3. **PIN照合は全有効スタッフに対する総当たりbcrypt検証方式**。Phase3-1では現状方式のまま採用(少人数運営を前提とした割り切り。スタッフ数増加時の性能は将来課題)
4. **`is_active`切替(退職者無効化等)の操作権限は、階層管理なしのため現状「ログイン済みスタッフなら誰でも操作可能」のまま**。Phase3-1では確定、将来的な権限拡張はPhase3-2以降で検討

### Gemini:実装担当
- 指定された担当領域のコード実装(SQLAlchemyモデル・Pydantic Schema・API・バリデーション・テスト)
- 共通設計から逸脱する必要がある場合は、勝手に変更せず「変更提案」として提示する
- 既存コードとの整合性を確認してから実装する

### Copilot:補助役(B案)
第三弾の主実装担当から外し、補助役として使用する。
- 担当可能:小規模なコード修正、文法・構文エラーの確認、エラー原因の調査補助、小規模なリファクタリング、仕様書・コードの軽微な整形、小規模な技術的壁打ち
- 担当しない:ER図の確定、DB構造の確定、テーブル設計の変更、API仕様の確定、共通命名規則の変更、AI間の担当範囲の変更、既存モデルの置換・削除、第三弾仕様の独自追加
- 設計上の問題を発見した場合は、勝手に修正せず「変更提案」として提示する

---

## 3. 第二弾 → 第三弾の既存資産接続ルール(確定)

第三弾は第二弾を作り直すのではなく、第二弾を土台として拡張する。

**User / staff**:別テーブルとして確定。既存の利用者管理`User`と第三弾のスタッフ管理`staff`は別物として扱う。

**LaneSet / lanes**:既存の`LaneSet`は変更せず維持する。第三弾では個別レーン管理のため、新規`lanes`テーブルを追加する。`lanes`は`lane_set_id`により`LaneSet`と紐付ける。

**ClassSession**:既存の第二弾`ClassSession`を置き換えない。既存の`class_session_id`, `course_id`, `session_number`, `date`, `start_time`, `end_time`, `status`は維持し、第三弾で必要な情報を追加カラムとして拡張する。

- **追加カラム:`lane_pair`(整数)**。使用するレーンペアの**先頭レーン番号**を保持する方式で確定(例:`lane_pair=1`→1・2番レーン、`lane_pair=3`→3・4番レーン、`lane_pair=5`→5・6番レーン)。DB値だけで使用レーンを判別できるため、ペア通し番号(1,2,3…)ではなくこの方式を採用する。Phase3-2の`lane_pair_assignment`テーブルの`lane_pair`カラムも同じ方式で統一する。

**class_group**:第三弾で新規`class_group`テーブルは作成しない。既存の`ClassCourse`(募集枠)・`ClassEnrollment`(個人申込)・`ClassGroupEnrollment`(職場等のグループ申込)で代替する。個人申込とグループ申込が既存モデルですでに分離されているため、新たな`class_group`は不要と判断した。この判断は当初仕様(仕様書での`class_group`新設案)からの変更であるため、変更理由をここに記録する。

**class_attendance**:第三弾で新規追加。既存の`class_sessions.class_session_id`をFKとして使用する。

**payment**:第三弾で新規追加。`payer_type`(class/checkin)に応じて`course_id`(教室支払い時)または`checkin_id`(時間貸し支払い時)を紐付ける。

**checkin**:第三弾で新規追加。

---

## 4. 既存モデルの扱い

第二弾で確定している既存モデルを基準とする:`User`, `LaneSet`, `Reservation`, `ClassCourse`, `ClassSession`, `ClassEnrollment`, `GroupReservationDetail`, `ClassGroupEnrollment`。これらと第三弾の新規モデルとの関係を確認してから実装し、同じ意味を持つテーブルを重複作成しない。

---

## 5. SQLAlchemy / Pydantic / API共通ルール

**SQLAlchemy**
- 既存モデルとのFK関係を確認する
- テーブル名とモデル名の対応を明確にする(snake_case テーブル、PascalCase クラス)
- status等の値は本文書7章で確定したものを使用する
- 既存テーブルを勝手に削除・置換しない

**Pydantic**
- API境界で入力値をバリデーションする
- DB側のCHECK制約と必要に応じて二重に守る
- 既存のバリデーション方針を尊重する
- 命名:`<Model>Create` / `<Model>Update` / `<Model>Read`

**API**
- REST形式、ベースパス`/api/v1/`、リソース名は複数形・snake_case
- 命名規則・レスポンス形式は共通設計に従う。AIごとに異なる形式を作らない

---

## 6. ディレクトリ構成

第二弾の既存構成を踏襲する。

```
app/
  database.py
  main.py
  models/
    models.py
  routers/
  schemas/
    schemas.py
  utils/
    auth.py
```

Phase3-1で新しいファイル・ディレクトリが必要になった場合も、既存構成との整合性を確認してから追加する。

---

## 7. 命名・status・Enum(確定)

- `lane.status`:`available` / `in_use` / `broken` / `maintenance`
- `checkin.status`:`checked_in` / `in_use` / `checked_out`
- `class_attendance.attendance_status`:`all_present` / `partial_absent_continue` / `partial_absent_slide`
- `payment.payer_type`:`class` / `checkin`
- `payment.payment_timing`:`prepaid` / `postpaid`
- `payment.payment_method`:`cash` / `card` / `e_payment` / `unpaid`
- `class_sessions.lane_pair`:先頭レーン番号方式(3章参照)
- `staff.pin_hash`:平文`pin_code`ではなくbcryptハッシュで保存(2章「staff実装の確定事項」参照)

仕様書に存在しない値を独自に追加しない。新しい値が必要な場合は「変更提案」として提示する。

---

## 8. 変更提案ルール

以下の場合は実装を止め、変更提案として提示する。

- 既存テーブルとの役割が重複する
- 新しいテーブルが必要か判断できない
- 既存モデルを変更する必要がある
- FK関係が仕様書と一致しない
- API仕様を変更する必要がある
- status / Enumを追加する必要がある
- 仕様書と既存コードが矛盾する
- 他AIの実装と設計が食い違っている

変更提案には必ず次の4点を示す:
1. 何を変更するか
2. なぜ必要か
3. 影響範囲
4. 変更しない場合の問題

**承認されるまで独断で実装しない。**

---

## 9. 実装順序

① 共通設計確認 → ② 担当範囲確認 → ③ 各AIが担当領域を実装 → ④ 動作確認 → ⑤ ChatGPTが全体を整理 → ⑥ Claudeが設計・整合性レビュー → ⑦ 問題があれば修正 → ⑧ Phase3-1完了

---

## 10. 最重要ルール

**「迷ったら勝手に作らない」**

仕様書・既存コード・共通設計のどれにも明確な答えがない場合、実装より先に質問または変更提案を行う。4AIがそれぞれ別の判断で設計を作らない。

---

## 11. AIチーム構成

```
純子さん
   ↓
ChatGPT(司令塔)
   ↓
┌─────────────┬─────────────┬─────────────┐
│ Claude      │ Gemini      │ Copilot     │
│ 設計+実装+レビュー│ 実装担当      │ 補助担当(B案) │
└─────────────┴─────────────┴─────────────┘
```

全員がこの憲法を共通ルールとして参照する。設計を勝手に変更しない。既存システムを勝手に壊さない。迷ったら変更提案。AI間で判断を分散させない。
